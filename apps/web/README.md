# Web application

Planned Next.js/TypeScript interface. `app` will hold routes and layouts, `components` reusable presentation, and `lib` typed API/client utilities. No framework has been initialized yet so generated defaults can be reviewed during the implementation milestone.
