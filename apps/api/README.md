# API application

Planned Python 3.12/FastAPI backend. The `src` layout prevents accidental imports from the repository root. No package configuration or application code is added in the architecture phase.

Tests are split into fast isolated `unit` tests and database/API-facing `integration` tests.
